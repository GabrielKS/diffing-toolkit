# @package model
name: gemma2_9B_it
model_id: google/gemma-2-9b-it
end_of_turn_token: <end_of_turn>
attn_implementation: null
token_level_replacement: null
dtype: bfloat16
ignore_first_n_tokens_per_sample_during_collection: 0 # This is for activation collection. 
ignore_first_n_tokens_per_sample_during_training: 0 # This is for any training on top of the collected activations (and applies on top of the ignore_first_n_tokens_per_sample_during_collection)

has_enable_thinking: false # The tokenizer has the enable_thinking parameter

# For generation: For some models nnsight seems to be buggy with compiled models.
disable_compile: true

# How the system_prompt of a prompted variant is injected: the Gemma 2 template rejects a
# system role outright, so prefixing the first user turn is the only workable mode.
system_prompt_mode: user_prefix
system_prompt_separator: "\n\n"
